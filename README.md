##BACK-END:

- Open the project in the IDE
- Open Docker Desktop
- Open the teminal `./backend` and run   `mvn package` to build the .jar
- Open the terminal `./frontend ` and run `npm install` to install all dependencies in frontend
- Go to project root and run on terminal `docker-compose up --build`
- In navigator open `localhost:5173`
